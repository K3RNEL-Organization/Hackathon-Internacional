# Solo para desarrollo local. En cualquier otro ambiente, generar un secreto
# propio y configurar el MISMO valor aca y en el backend (RISA_JWT_SECRET),
# ya que el frontend verifica los JWT que firma el backend.
RISA_API_URL=http://localhost:8000
RISA_JWT_SECRET=risa-data-dev-secret-change-me
